# bioplastic-designer
An inverse-design tool that finds the combination of materials that produce a set of desired properties. It uses Gaussian Process models to predict a material's properties from its composition, then searches for the composition whose predicted properties best match the target.
